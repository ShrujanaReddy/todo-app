name: Node.js CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20.10.0"

      - name: Build Docker image
        run: cd todo-app && docker-compose build

      - name: Run Docker Compose file
        run: cd todo-app && docker-compose up -d && sleep 10

      - name: Run unit tests
        run: cd todo-app && docker-compose run --rm app npm test

      - name: Run integration tests
        run: |
          cd todo-app  
          docker-compose run --rm app npx sequelize-cli db:drop  
          docker-compose run --rm app npx sequelize-cli db:create  
          docker-compose run --rm app npx sequelize-cli db:migrate  
          docker-compose run --rm app npm run start:dev &
          sleep 10  
          docker-compose run --rm app npm install cypress cypress-json-results  
          docker-compose run --rm app npx cypress run

      - name: Stop and remove Docker containers
        run: cd todo-app && docker-compose down

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SERVER_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan github.com >> ~/.ssh/known_hosts

      - name: Deploy to the server
        uses: appleboy/ssh-action@v0.1.5
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            docker pull your-docker-repo/todo-app:latest
            docker stop todo-app || true
            docker rm todo-app || true
            docker run -d --name todo-app -p 80:80 your-docker-repo/todo-app:latest
